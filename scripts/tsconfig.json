{
  "//": "am-7mp8: scripts/ is outside the program tsconfig.json checks, so 179 of its 195 .ts files were never typechecked and 254 strict errors accumulated there, including 48 in the gate runner. This config runs the repository's own strict options over scripts/ as well, so the debt can be measured and driven down. It loosens nothing: compilerOptions are inherited unchanged. When the count reaches zero, fold scripts/**/*.ts into the root tsconfig include and delete this file.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "..",
    "paths": { "@/*": ["./src/*"] },
    "noEmit": true
  },
  "include": ["../next-env.d.ts", "../src/**/*.ts", "../src/**/*.tsx", "./**/*.ts"]
}
