{
  "//": "SUPERSEDED on 2026-09-17. The count reached zero and scripts/**/*.ts now sits in the root tsconfig include, so `bun run typecheck` checks this tree directly and this file adds nothing. Its header used to say to delete it at zero; AGENTS.md Rule 1 forbids deleting a file without the user's express written permission, so it stays until they say otherwise. Nothing references it.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "..",
    "paths": { "@/*": ["./src/*"] },
    "noEmit": true
  },
  "include": ["../next-env.d.ts", "../src/**/*.ts", "../src/**/*.tsx", "./**/*.ts"]
}
