/**
 * Said with a laboratory's refusal (dispatch 170, am-lab-domains-silently-clamped-pzj5). A refused
 * field keeps the text the reader typed, beside results that still belong to the last accepted
 * settings, so the refusal says which is which. src/testing/labFormSweep.test.tsx looks for
 * "last accepted" in every refusal a form shows.
 */
export const KEPT_RESULT = "The results shown are still those of the last accepted settings.";
