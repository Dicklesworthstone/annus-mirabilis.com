/**
 * The role vocabulary (am-design-semantic-color-8vbq) is imported from the
 * single source of truth -- `COLOR_ROLES` in
 * `src/content/schemas/meanings.ts`, which the quantity registry
 * (am-not-quantity-registry-2f7) validates against -- never redefined here.
 * "energy" | "time-rate" | "space-geometry" | "material" | "statistical" |
 * "field" | "quanta" is a contextual convention for following one quantity
 * across views, not a claim about the physics.
 */
export { COLOR_ROLES, type ColorRole } from "../../content/schemas/meanings";
