{
  "name": "annus-mirabilis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": "22.13.4"
  },
  "scripts": {
    "prepare:lab": "node --experimental-strip-types scripts/generate-lab.mjs && node --experimental-strip-types scripts/generate-tracers.mjs && node --experimental-strip-types scripts/generate-walks.mjs && node --experimental-strip-types scripts/generate-inference.mjs",
    "dev": "npm run prepare:content && npm run prepare:lab && next dev",
    "build": "npm run prepare:content && npm run prepare:lab && next build",
    "typecheck": "npm run prepare:content && npm run prepare:lab && tsc --noEmit",
    "test:reference": "npm run prepare:content && npm run prepare:lab && node --experimental-strip-types --test src/testing/*.test.mjs",
    "test:browser": "node scripts/test-lab-browser.mjs",
    "prepare:content": "node --experimental-strip-types scripts/build-content.ts && node --experimental-strip-types scripts/build-equations.ts"
  },
  "dependencies": {
    "next": "15.5.25",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "katex": "0.18.4"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "@types/node": "22.13.4",
    "@types/react": "19.0.0",
    "@types/react-dom": "19.0.0",
    "@types/katex": "0.16.7",
    "playwright": "1.62.1",
    "@axe-core/playwright": "4.10.1"
  },
  "overrides": {
    "postcss": "8.5.26"
  }
}
