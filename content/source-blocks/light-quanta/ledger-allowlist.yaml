# Acknowledged validator warnings for the ap-17-132 ledger.
#
# An entry says an editor looked at a warning and judged the LEDGER correct. It silences
# nothing: each names its line by fingerprint, so it goes stale the moment that line changes
# and the validator reports it as a stale entry.
#
# Every entry is acknowledged by agent:LilacValley, which is NOT a human editor. These are
# machine-draft acknowledgements and a German source reviewer should re-judge them with the
# text; docs/OWNERS.md still lists open-german-source-light-quanta as "open: recruiting".
#
# THE WARNING BEING ACKNOWLEDGED. `short-line` exists to catch a real defect named in
# AGENTS.md - "sentences broken into one-word lines", the OCR fragmentation that shreds
# prose into stubs. Einstein prints genuine one-word connectives between consecutive display
# equations, and LEDGER_FORMAT.md requires `$$` to open its own line, so a short connective
# introducing a display has nowhere else to sit. Reflowing it into a neighbouring sentence
# would be inventing text to quiet a heuristic.
#
# NOTE ON FORMAT: reasons are single-line because the provenance YAML reader
# (src/content/provenance/yaml.ts) is hand-rolled and does not support folded block scalars.
#
# NOTE ON LOCATION: validateLedger requires this file at exactly
# content/source-blocks/<slug>/ledger-allowlist.yaml. Until 0fbaa9a0 the bilingual reader
# treated every non-manifest yaml in this directory as a source block and served the
# ap-17-549 allowlist to readers as German source text. That is fixed - a source block must
# now be an object carrying a string id, and this file is a YAML sequence - but anything
# else placed here should be checked against that loader before it lands.

- ledgerKey: ap-17-132
  code: short-line
  ledgerPage: 5
  lineFingerprint: ad6f70890a12d8f2
  reason: "Printed connective 'wobei' standing alone between the Planck radiation formula and the two constant values that follow it on page 136. Source text, not an OCR line fragment; the format requires the following $$ to begin its own line, so the word cannot share a line with the display it introduces."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-132
  code: short-line
  ledgerPage: 6
  lineFingerprint: b81d9593a3f6c4f7
  reason: "Printed connective 'oder' standing alone between the coefficient identity and the value of N on page 137. Source text, same reasoning as the 'wobei' entry: the format requires the following $$ to begin its own line."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-132
  code: short-line
  ledgerPage: 7
  lineFingerprint: 4da271bb37e5a8c6
  reason: "Printed connective 'wenn' standing alone between the two variational conditions on page 138 - the entropy maximum and the energy constraint it is subject to. Source text, and load-bearing: it is the word that makes the second display a CONDITION on the first rather than a second independent statement."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-132
  code: short-paragraph
  ledgerPage: 11
  lineFingerprint: bb8c8605c55dcc2d
  reason: "Printed connective 'also' standing alone between the two display equations INSIDE footnote 1) on page 142, where Einstein derives p v = R (n/N) T from the entropy expression. Same class as the wobei/oder/wenn entries and the same reasoning, with one difference worth recording: it is reported as short-paragraph rather than short-line because a footnote's connective opens its own block, so a reviewer looking only for short-line will not find it."
  editor: "agent:LilacValley"
  date: "2026-09-21"
