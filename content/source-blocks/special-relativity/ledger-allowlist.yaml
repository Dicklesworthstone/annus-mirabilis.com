# Acknowledged validator warnings for the ap-17-891 ledger.
#
# An entry says an editor looked at a warning and judged the LEDGER correct. It silences
# nothing: each names its line by fingerprint, so it goes stale the moment that line changes
# and the validator reports it as a stale entry.
#
# Every entry is acknowledged by agent:LilacValley, which is NOT a human editor. These are
# machine-draft acknowledgements and a German source reviewer should re-judge them with the
# text; docs/OWNERS.md still lists open-german-source-relativity as "open: recruiting".
#
# THE WARNING BEING ACKNOWLEDGED. `short-line` exists to catch a real defect named in
# AGENTS.md - "sentences broken into one-word lines", the OCR fragmentation that shreds
# prose into stubs. Einstein prints genuine one-word connectives between consecutive display
# equations, and LEDGER_FORMAT.md requires `$$` to open its own line, so a short connective
# introducing a display has nowhere else to sit. Reflowing it into a neighbouring sentence
# would be inventing text to quiet a heuristic.
#
# NOTE ON FORMAT: reasons are single-line because the provenance YAML reader
# (src/content/provenance/yaml.ts) is hand-rolled and does not support folded block scalars.
#
# NOTE ON FINGERPRINTS: computeLineFingerprint hashes the line TEXT alone, so two identical
# one-word lines share a fingerprint and only ledgerPage distinguishes their entries. A
# one-word line's fingerprint pins what the line says, not which line it is.

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 7
  lineFingerprint: 856cc99d87343400
  reason: "Printed connective 'und' standing alone at the head of page 897, joining the two light-time equations of the synchrony argument across the page break: page 896 ends with t_B - t_A = r_AB/(V - v) and 897 opens with this 'und' and then t'_A - t_B = r_AB/(V + v). Source text, and load-bearing: the word is what makes the second equation a conjunct of the first rather than a separate statement, which is the step the section 2 conclusion rests on."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 10
  lineFingerprint: ad6f70890a12d8f2
  reason: "Printed connective 'wobei' standing alone between displays on page 900, in the derivation of the transformation equations. THIS ENTRY ACKNOWLEDGES TWO LINES, not one: 'wobei' occurs twice on this page (before the auxiliary condition y/sqrt(V^2-v^2)=t, and before the definition of beta) and computeLineFingerprint hashes the line TEXT alone, so both warnings match this single entry and neither the file nor the validator can tell them apart. An editor acknowledging one has acknowledged both. Both are source text and both introduce a display that LEDGER_FORMAT requires to open its own line."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 10
  lineFingerprint: bb8c8605c55dcc2d
  reason: "Printed connective 'also' standing alone between the auxiliary condition and the expression for eta on page 900. Source text, same reasoning as the wobei entries. Note that this fingerprint is byte-identical to the 'also' entry in the light-quanta allowlist, since the hash is of the line text; ledgerKey and ledgerPage are what separate them."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 14
  lineFingerprint: 856cc99d87343400
  reason: "Printed connective 'und' standing alone between the two clock equations on page 904, section 4: tau = (1/sqrt(1-(v/V)^2))(t - (v/V^2)x), then 'und', then x = v t. Source text. Its fingerprint is byte-identical to the page 897 'und' entry because computeLineFingerprint hashes the line text alone; ledgerPage 14 versus 7 is the only thing separating the two entries, and an editor must judge each page's line on its own plate."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-891
  code: short-paragraph
  ledgerPage: 18
  lineFingerprint: ad6f70890a12d8f2
  reason: "Printed connective 'wobei' standing alone between two displays on page 908, introducing the definition of beta. It is reported as a short PARAGRAPH rather than a short line only because 908 is one of two pages in any ledger whose first body line opens a display: the paragraph carried over from 907 begins on this page with the last two rows of the Maxwell-Hertz transform, so the only prose the paragraph has on this page is that one word. The word is source text and load-bearing - it is what makes the beta display a definition used by the equations above rather than a free-standing assertion. Denominator: 63 pages with a body across the four ledgers, 2 of which open with a display (ap-17-549 p551 and this one)."
  editor: "agent:LilacValley"
  date: "2026-09-21"

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 21
  lineFingerprint: ad6f70890a12d8f2
  reason: "Printed connective 'wobei' standing alone on page 911, between the transformed wave display and the four definitions of omega-prime, a-prime, b-prime and c-prime that 'gesetzt ist.' closes. Source text. Note the contrast with the page 908 entry, which acknowledges the SAME WORD under a different code: there 'wobei' was the paragraph's last text line and the rule reported it as a short PARAGRAPH, here it is followed by 'gesetzt ist.' in the same paragraph and the rule reports a short LINE. Same printed word, same reason to keep it, two codes, decided by what follows it rather than by anything about the word."
  editor: "agent:LilacValley"
  date: "2026-09-22"

- ledgerKey: ap-17-891
  code: short-line
  ledgerPage: 26
  lineFingerprint: ad6f70890a12d8f2
  reason: "Printed connective 'wobei' standing alone between displays on page 916, section 9. THIS ENTRY ACKNOWLEDGES TWO LINES: 'wobei' after the Maxwell-Hertz equations with convection current (before rho = dX/dx + dY/dy + dZ/dz), and 'wobei' after the transformed system (before the u_xi, u_eta, rho', u_zeta group). The fingerprint hashes the line text alone, so both match this one entry. Both stand flush on their own line in the plate (read at 300 and 600 dpi), are source text, and introduce a display that LEDGER_FORMAT requires to open its own line."
  editor: "agent:GreenBarn"
  date: "2026-09-25"
